const useRoutes = () => {
  return {
    home: "/",
    menu: "/menu",
    section: "/section",
    assets: "/assets",
    configuration: {
      machines: "/machines",
      spareParts: "/spareParts",
    },
  };
};

export default useRoutes;
